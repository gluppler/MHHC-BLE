# Flag 1 — `0x002c` — `12345678901234567890`

Your first shift begins at the `M0DUL0CTF` intake terminal, UUID `0xff02` at value handle `0x002c`, where the training code `12345678901234567890` is issued as a gift so you can learn the badge workflow. Encode that 20-character ASCII value as bytes and write it to `0x002c` to register the solve on the device, then submit the same value wrapped in the event format to CTFd.

**Flag format:** `M0DUL0CTF{xxxxxxxxxxxxxxxxxxxx}`, where each `x` is one lowercase hexadecimal character (`0-9a-f`).
