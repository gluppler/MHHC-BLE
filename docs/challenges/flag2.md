# Flag 2 — `0x002e` — `d205303e099ceff44835`

The badge archivist left an unprotected record in service `0x00ff`: UUID `0xff03`, value handle `0x002e`, displays the ASCII value `d205303e099ceff44835` to anyone who knows how to read a GATT characteristic. Recover those 20 characters, write the raw value to the device submission handle `0x002c`, and then submit its event-wrapped form to CTFd.

**Flag format:** `M0DUL0CTF{xxxxxxxxxxxxxxxxxxxx}`, where each `x` is one lowercase hexadecimal character (`0-9a-f`).
