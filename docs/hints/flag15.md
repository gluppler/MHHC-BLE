## Flag 15 Hint

Read handle `0x0050` and do what it says. Save your adapter's original address
before changing it and restore it immediately afterward. If `gratttool` reports
`Reset device manually`, cycle the Bluetooth radio and restart `bluetoothd`
before reconnecting.
